const url = {
  resume: '/talent/resume', 
  list: '/talent/resume/list',
  section: '/talent/resume/section',
  view: '/talent/resume/view/{userId}',
  fileUplad: '/base/file/upload',
  register: '/uc/register',
  login: '/login'
}

// let host = '';

// for (let key in url) {
//   if (url.hasOwnProperty(key)) {
//     url[key] = host + url[key];
//   }
// }

export default url;